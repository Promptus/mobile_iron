require "spec_helper"

describe MobileIron do
  it "has a version number" do
    expect(MobileIron::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
